import React from "react";
import guide from "../data/guide";
import Link from "next/link";
import Image from "next/image";

<div style={{ 
        'width': '100%', 
        'height': '100%', 
        'display': 'flex', 
        'flexDirection': 'column', 
        'alignItems': 'center' 
    }}
>
    {guide.map(blog => (
        <div key={'container'}
            style={{
                'width': '100%',
                'height': 'fit-content',
                'padding': '0.75rem',
                'background': 'transparent',
                'borderRadius': '0.5rem',
                'border': '1px dashed rgb(255,0,64)',
                'marginTop': '1.75rem',
                'cursor': 'pointer',
            }}
        >
            <Link href={blog.link}
                style={{
                    'display': 'flex',
                    'flexDirection': 'row',
                }}
            >
                <div key={'text'}
                    style={{
                        'display': 'flex',
                        'flexDirection': 'column',
                        'justifyContent': 'space-between',
                        'marginLeft': '0.5rem'
                    }}
                >
                    <div key={'title'}
                        style={{
                            'fontSize': '1.5rem',
                            'lineHeight': '2rem',
                            'fontWeight': 'bold',
                        }}
                    >
                        {blog.title}
                    </div>
                    <div key={'publishedAt'}
                        style={{
                            'fontSize': '1rem',
                            'lineHeight': '1.5rem',
                            'fontWeight': '600',
                        }}
                    >
                        {blog.publishedAt}
                    </div>
                </div>
            </Link>
        </div>
    ))}
</div>
